{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "outputs": ["build/**"]
    },
    "test": {
      "dependsOn": ["lint", "build"]
    },
    "lint": {}
  }
}
